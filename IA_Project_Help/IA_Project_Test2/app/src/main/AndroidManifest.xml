<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cis.ia_project_test">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IA_Project_Test">
        <activity android:name=".BillInfoActivity"></activity>
        <activity android:name=".TypeActivity" />
        <activity android:name=".AddBillActivity" />
        <activity android:name=".AllBillActivity" />
        <activity android:name=".allSubActivity" />
        <activity android:name=".AddSubActivity" />
        <activity android:name=".signUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".authActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlertReceiver" />
    </application>

</manifest>